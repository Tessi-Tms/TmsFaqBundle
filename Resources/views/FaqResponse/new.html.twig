{% extends 'TmsComplaintBundle::layout.html.twig' %}

{% block nav %}
    {% include 'TmsComplaintBundle::nav.html.twig' with {'active' : ['faq', 'response']} %}
{% endblock %}

{% block main %}
    <h1>{% trans %}Faq response creation{% endtrans %}</h1>

    <form action="{{ path('tms_faq_faq-response_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit" class="btn">{% trans %}Create{% endtrans %}</button>
        </p>
    </form>

    <ul class="actions">
        <li>
            <a href="{{ path('tms_faq_faq-response') }}" class="btn">
                {% trans %}Back to the list{% endtrans %}
            </a>
        </li>
    </ul>
{% endblock %}
